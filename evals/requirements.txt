# Eval deps. End-to-end runner hits the orchestrator over HTTP and judges with
# Claude; the retrieval scorecard calls shared.retrieval directly (hence the
# vector-store deps + shared/ in the image).
anthropic==0.105.2
requests==2.34.2
voyageai==0.3.7
qdrant-client==1.15.1
pydantic==2.13.4
